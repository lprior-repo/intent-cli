{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://intent-cli.dev/schemas/quality-response.json",
  "title": "Intent CLI Quality Response",
  "description": "JSON response schema for 'intent quality' command",
  "allOf": [
    {
      "$ref": "base-response.json"
    },
    {
      "properties": {
        "action": {
          "const": "quality_report"
        },
        "command": {
          "const": "quality"
        },
        "data": {
          "$ref": "#/definitions/QualityData"
        }
      }
    }
  ],
  "definitions": {
    "QualityData": {
      "type": "object",
      "required": [
        "overall_score",
        "coverage_score",
        "clarity_score",
        "testability_score",
        "ai_readiness_score",
        "issues",
        "suggestions"
      ],
      "properties": {
        "overall_score": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "Aggregate quality score"
        },
        "coverage_score": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "Test coverage completeness"
        },
        "clarity_score": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "Specification clarity"
        },
        "testability_score": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "How testable the spec is"
        },
        "ai_readiness_score": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "Readiness for AI implementation"
        },
        "issues": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Identified problems"
        },
        "suggestions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Improvement recommendations"
        }
      }
    }
  }
}
