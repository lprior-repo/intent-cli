#!/bin/bash
# Intent CLI wrapper
# Requires: Erlang runtime, CUE tool in PATH

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/build/erlang-shipment/entrypoint.sh" run "$@"
