$schema: './.moon/cache/schemas/project.json'

# Rust workspace root
# All tasks defined in .moon/tasks.yml (workspace-level)
stack: system

# Empty tasks - defer to workspace configuration
tasks: {}
